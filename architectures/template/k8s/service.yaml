apiVersion: v1
kind: Service
metadata:
  name: template-service
  namespace: template-arch
spec:
  selector:
    app: template-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
