apiVersion: apps/v1
kind: Deployment
metadata:
  name: monolithic-app
  namespace: monolithic-arch
spec:
  replicas: 1
  selector:
    matchLabels:
      app: monolithic-app
  template:
    metadata:
      labels:
        app: monolithic-app
    spec:
      containers:
        - name: monolithic-app
          image: node:20-alpine
          command: ["node", "-e", "const http = require('http'); http.createServer((req, res) => { res.writeHead(200); res.end('Monolithic pattern working'); }).listen(8080);"]
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
