# GitHub Container Registry token for host-side docker login.
# Required if ghcr.io/glitchyi/blog:latest is private.
CR_PAT="ghp_replaceWithGitHubPackageReadToken"

# Watchtower webhook secret.
# Must match the WATCHTOWER_TOKEN GitHub Actions secret.
WATCHTOWER_TOKEN="your-random-webhook-token"
